_schema-version: "3.1"
ID: iwonahdemo
description: A simple CAP project.
version: 1.0.0
modules:
- name: iwonahdemo-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: iwonahdemo-db
  - name: iwonahdemo-uaa
  - name: iwonahdemo-logs
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm-ci
- name: iwonahdemo-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: iwonahdemo-db
  - name: iwonahdemo-logs
  parameters:
    buildpack: nodejs_buildpack
- name: iwonahdemo-destinations
  type: com.sap.application.content
  requires:
  - name: iwonahdemo-uaa
    parameters:
      service-key:
        name: iwonahdemo-uaa-key
  - name: iwonahdemo-html5-repo-host
    parameters:
      service-key:
        name: iwonahdemo-html5-repo-host-key
  - name: srv-api
  - name: iwonahdemo-destination
    parameters:
      content-target: true
  parameters:
    content:
      instance:
        destinations:
        - Authentication: OAuth2UserTokenExchange
          Name: iwonahdemo-app-srv
          TokenServiceInstanceName: iwonahdemo-uaa
          TokenServiceKeyName: iwonahdemo-uaa-key
          URL: ~{srv-api/srv-url}
          sap.cloud.service: iwonahdemo.service
        - Name: iwonahdemo-html5-repo-host
          ServiceInstanceName: iwonahdemo-html5-repo-host
          ServiceKeyName: iwonahdemo-html5-repo-host-key
          sap.cloud.service: iwonahdemo.service
        - Authentication: OAuth2UserTokenExchange
          Name: iwonahdemo-uaa
          ServiceInstanceName: iwonahdemo-uaa
          ServiceKeyName: iwonahdemo-uaa-key
          sap.cloud.service: iwonahdemo.service
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: iwonahdemo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: iwonahdemo-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsrisks.zip
      name: nsrisks
      target-path: resources/
    - artifacts:
      - nsmitigations.zip
      name: nsmitigations
      target-path: resources/
- name: nsrisks
  type: html5
  path: app/risks
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: nsmitigations
  type: html5
  path: app/mitigations
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: iwonahdemo-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-service-name: ${service-name}
- name: iwonahdemo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      role-collections:
      - description: Manage Risks
        name: RiskManager-${space}
        role-template-references:
        - $XSAPPNAME.RiskManager
      - description: View Risks
        name: RiskViewer-${space}
        role-template-references:
        - $XSAPPNAME.RiskViewer
      tenant-mode: dedicated
      xsappname: iwonahdemo-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: iwonahdemo-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-plan: lite
- name: iwonahdemo-logs
  type: org.cloudfoundry.managed-service
  parameters:
    service: application-logs
    service-plan: lite
- name: iwonahdemo-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx -p @sap/cds-dk cds build --production
    - npx rimraf gen/db/src/gen/data